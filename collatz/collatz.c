#include <ctype.h>
#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void);

int n;
    {
    if (n == 1)
    return 0;

    else if ((n % 2) == 0)
    return 1 + n(n/2);

    else
    return 1 + n(3 * n + 1);
    }